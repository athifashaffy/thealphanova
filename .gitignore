
# Server-side secrets — never commit (the repo is public)
.env

# python bytecode from ops/ generators
__pycache__/

# local browser traces from cloud agents
.playwright-mcp/

# Bid drafts with prices and client contacts. Public repo. Keep them in Drive.
ops/rfp-workflow/review-pack/
